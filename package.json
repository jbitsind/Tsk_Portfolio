{
  "name": "tsk-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html src/blog/*.html --dist-dir dist --public-url ./",
    "dev": "parcel src/index.html --dist-dir dist --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.16.4",
    "@parcel/resolver-default": "^2.16.4"
  }
}
